version: "3"
services:
    # reverse-proxy:
    #     build: ./reverse-proxy/.
    #     image: coding-militia-docker-reverse-proxy:latest
    #     ports:
    #         - "5000:80"
    #         - "5001:5001"
    #     networks:
    #     - overlay
    #     depends_on:
    #         - frontend
    frontend:
        build: ./Frontend/.
        image: frontendimg:latest
        # networks:
        #     - overlay
        depends_on:
            - backend
    backend:
        build: ./Backend/.
        image: backendimg:latest
        ports:
            - "8000:80"
        # networks:
        #     - overlay
        # depends_on:
        #     - postgres-db
    # postgres-db:
    #     image: "postgres"
    #     networks:
    #         - overlay
    #     environment:
    #         POSTGRES_USER: "user"
    #         POSTGRES_PASSWORD: "pass"

# networks:
#   overlay: